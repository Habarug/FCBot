import discord

class GuildIDs:
    """The unique Server IDs."""

    MMM = 775313216682852352
    # This is done for application commands, we need a list of guild objects.
    ALL_GUILDS = [discord.Object(id=MMM)]
